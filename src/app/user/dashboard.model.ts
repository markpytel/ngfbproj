import { Url } from "url";

export class Client {
    clientId: number
    clientName: string
    clientPassword: password
}

export class URL {
    clientId: number
    url: url
    thumbnail: img
    status: string
    priority: string
}